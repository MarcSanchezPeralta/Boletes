En aquet programa farem sortir un rectangle quan volgem

int a = 0;
int b = 0;
int c = mouseX;
int d= mouseY ;
void setup(){
  size(500,500);
  
}
void draw(){
  background(0);
  rect(a,b,mouseX -a,mouseY-b);
}
  void mouseClicked(){
    a= mouseX;
    b= mouseY;
  }
  
  
